"""Helper script (prototype) to create an approver.
In this minimal prototype approvers are represented by email/role and not persisted as a full ACL.
Extend as needed."""
print("This prototype uses simple role names in the approval request (e.g., 'hr_manager').")
print("Implement a user/role store and an SSO flow to map approver_id to user identity for production.")

version: "3.8"
services:
  web:
    build: .
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    depends_on: []
# Note: You can add a postgres service here and set DATABASE_URL accordingly.
